# LaterMD 架构决策记录

本目录存放项目的技术决策记录（ADR）。每条 ADR 的结论都由**实测数据**支撑，不是推测。

## 文档索引

| 文档 | 主题 | 状态 |
|---|---|---|
| [adr-001-gui-and-architecture.md](adr-001-gui-and-architecture.md) | GUI 框架选型、workspace 分层、里程碑 | 已接受 |
| [adr-002-platform-renderer-wysiwyg.md](adr-002-platform-renderer-wysiwyg.md) | 平台基线、图形后端、WYSIWYG 形态 | 已接受 |
| [adr-003-renderer-and-ecosystem-audit.md](adr-003-renderer-and-ecosystem-audit.md) | 渲染层与 Rust 生态审计 | 已接受 |
| [adr-004-technical-stack.md](adr-004-technical-stack.md) | 技术栈与依赖版本清单、打包方案、工具链 | 已接受（2026-09-24 重构） |
| [adr-005-layout-and-sidebar.md](adr-005-layout-and-sidebar.md) | 三栏布局、侧边栏、字体与并发模型 | 已接受 |
| [roadmap.md](roadmap.md) | 阶段划分、验收标准、crate 增量时机、风险登记册、当前位置 | 已接受（**排期唯一事实来源**） |
| [vendor-upgrade-checklist.md](vendor-upgrade-checklist.md) | egui_markdown vendor 与升级操作清单 | **已执行**(2026-09-24 六项全绿) |
| [m0-report.md](m0-report.md) | M0 技术验证结论（IME / 帧率 / 三 target 实测数据） | 已产出（Linux 项完成；IME 与 Win·mac wgpu 待真机） |
| [p0-acceptance.md](p0-acceptance.md) | P0 四条验收标准的逐条状态、证据出处与出口待办 | 跟踪中（功能 10/11，剩打包首跑验证） |
| [ui-design.md](ui-design.md) | 界面设计规范：需求表达词库 + 实测诊断 + Design Token + 组件规范 | 已接受（roadmap 专题批次 B 的视觉细化） |

## 决策总表（一句话版）

| 议题 | 结论 | 出处 |
|---|---|---|
| GUI 框架 | **egui + eframe 0.36.2** | ADR-001 §2 |
| Markdown 解析 | **pulldown-cmark**（不是 comrak） | ADR-003 §4 |
| 渲染层 | **vendored egui_markdown (membrane-io)**，升级到 egui 0.36.2 | ADR-003 §2、§6 |
| 图形后端 | **wgpu**（eframe 默认），glow 仅作 env-var 逃生口 | ADR-002 §3 |
| 平台基线 | Windows 11 / macOS 14；不支持 Win10、macOS 13 | ADR-002 §2 |
| WYSIWYG 形态 | **B：Live Preview**；True WYSIWYG 列入 non-goals | ADR-002 §4 |
| 打包编排 | **axodotdev/cargo-dist** v0.33+ | ADR-004 §4 |
| 工具链 | **rustc 1.98.0** 钉死 | ADR-004 §9 |
| 布局 | `Panel::left` × 2 + `CentralPanel`；**不用 `SidePanel`**（0.36.2 已移除） | ADR-005 §2 |
| App trait | `logic` / `ui` 二分；**无 `update`** | ADR-005 §2.3 |
| 侧边栏折叠 | `Panel::show_collapsible`（非手写 `bool`） | ADR-005 §3.3 |
| 搜索引擎 | `ignore` + `grep-searcher` + `regex`；**不用 `grep-regex`** | ADR-005 §4.2 |

## 三条贯穿全局的约束

1. **单一解析器。** 全项目只允许存在一个 Markdown 解析器（pulldown-cmark），避免预览与导出出现两套方言。
2. **`core` 不依赖 UI 框架。** 渲染层只产出绘制指令，不 import `egui`，以便将来做 headless CLI 导出器。
3. **AST 是 AI 增强的架构基础。** 所有 AI 修改在 token/AST 层操作，而非操作渲染后的盒子。

## 修订记录

| 日期 | 变更 |
|---|---|
| 2026-09-24 | 初版：ADR-001/002 建立，GUI 与平台决策 |
| 2026-09-24 | ADR-003：生态审计。推翻「egui_markdown 已死」的初判（实际是仓库迁至 membrane-io 并持续活跃）；推翻「单 Galley 无法虚拟化」（实际已实现视口剔除） |
| 2026-09-24 | ADR-004 + roadmap：修正打包工具（cargo-dist 未归档的是 astral 分支，官方为 axodotdev）；大幅削减功能范围 |
| 2026-09-24 | vendor-upgrade-checklist：0.34→0.36.2 升级实测（24 个错误），并发现其中 9 个是上游 HEAD 自身 bug（在其自称支持的 egui 0.34.3 上也编译失败） |
| 2026-09-24 | ADR-005 + roadmap：三栏布局。修正 `SidePanel` / `App::update` 两个已消失的 API；大纲从 P3 前移到 P0（廉价版 3 天）；`Token::source_span` 提前到 vendor 阶段一次性完成 |
| 2026-09-24 | **文档体系一致性修订**：① ADR-004 重构为纯技术栈 ADR（原「最终汇总」中的功能范围/排期是过期快照，与 roadmap 冲突，已移交）；② 确立 roadmap.md 为排期唯一事实来源，ADR-001 §5 加修订注；③ P3「全文检索」与 P1 侧边栏搜索重复，删除；④ roadmap 阶段 0 验收补齐第六项 clippy；⑤ 新增 crate 增量创建表、风险登记册、阶段出口条件、M0 产出物约定（m0-report.md）；⑥ 修复 checklist 笔误 |
| 2026-09-24 | **分发策略修订**：确认无 Apple Developer 账号与签名证书。分发改为 GitHub Release + Homebrew tap `crazykun/homebrew-ailater`（cask 复刻 lscreen 模式：universal2 dmg + postflight 去 quarantine + livecheck）。签名公证相关结论在 ADR-004 / roadmap / AGENTS.md 中同步作废改写 |
| 2026-09-24 | **阶段 0（Vendor 适配）完成**：subtree 引入 egui_markdown@4f3075f，egui 0.34→0.36.2（15 错误 + 未预见的 TexturesDelta drop 检查 10 测试），删除 membrane feature（23 处），`Markdown::spans` 平行数组落地（大纲/Live Preview prerequisite）。check.sh 六项全绿、100 测试通过、latermd-app 空窗口在 Linux/X11 实跑通过 |
| 2026-09-24 | **M0 中间态**：`docs/m0-report.md` 产出（Linux 侧三项通过 —— wgpu/Vulkan 软件 adapter 起动、流式追加 ~9 ms、中文渲染无方块且字体方案定案为 cfg 原生候选表）；bench 全量重跑数据回填。CI 重写为 gate（六项门禁 + glow 编译）+ 三平台 build 矩阵，工具链钉 1.98.0。剩余真机项（IME、Win11 DX12 / macOS Metal）未测，M0 结论未最终放行 |
| 2026-09-24 | **roadmap：新增「界面美化与皮肤系统」专题**。批次 A（Light/Dark 双皮肤 + 外壳/正文/代码高亮三层联动 + 持久化）并入 P0 主题条目并扩至 1.5 周；批次 B（三态切换 + 自定义皮肤文件 + 视觉打磨）设为阶段 4.5（P2.5，P3 前视觉定稿）；批次 C（皮肤市场等）明确不做。实测盘点：vendored `MarkdownStyle` 原生成对明暗字段、`code_theme` 不传自动跟随明暗、缓存 hash 含 `dark_mode`，皮肤系统全程不动 vendor。风险登记册新增 #8（Linux 跟随系统主题无统一规范） |
| 2026-09-25 | **P0 模块批量实现 + M0 报告产出**：latermd-md（解析与大纲数据层）、三栏布局骨架、源码编辑与实时预览、文件新建/打开/保存/另存、大纲面板与光标跳转、HTML 导出（latermd-export）、主题切换与持久化、统一快捷键与菜单栏、文件树基础版，另含大纲跳转 span 钳制与保存/导出原子落盘修复（共 11 commits）。M0 报告补齐中文字体注入落地、全量 bench 数据与六项验证状态；roadmap「当前位置」更新为 M0 真机项挂账、P0 进行中 |
| 2026-09-25 | **新增 [ui-design.md](ui-design.md) 界面设计规范**。实测取证：逐点采样像素值（应用窗口截图）与 egui 0.36.2 出厂常量比对，确认整屏色值（`panel_fill` gray 27 / `extreme_bg_color` gray 10 / `code_bg_color` gray 64 / `Selection` rgb(0,92,128)）**全部为框架默认**，`theme.rs` 未做任何 token 覆盖 —— 定位为「从未做过视觉设计」而非「配色选错」。文档含六类病因词库、形容词→参数对照表、Design Token 定稿（明暗双套，对比度已核对）、11 条病征与组件级规范。**提出把 token 化提前到 P0.5（1.5 天）的建议，待拍板** |
| 2026-09-25 | **P0 收尾修补三模块完成**：① CJK 字体候选表补全三平台路径（Windows msyh.ttc/simhei.ttf/msyhbd.ttc + macOS PingFang.ttc/Hiragino Sans GB.ttc，平台前缀互斥 + is_file 探测分流，单候选读取失败跳下一候选，新增 3 项 fonts 单测，`8f1a556`）；② ADR-004 §2 补登 serde/serde_json 两行（主题 settings.json 与文件树 file_tree.json 持久化用途）并订正 latermd-app Cargo.toml 过时注释（`9b4b593`）；③ 设置菜单显示当前渲染后端（wgpu/glow，与启动选择同规则镜像判定）（`469eca9`）。auto-plan 功能队列 #1 p0-fixes 随之标记 ✅完成；真机项（Win/mac 字体存在性与渲染、GUI 后端显示肉眼核对）仍挂账人工 |
| 2026-09-25 | **P1 全文搜索落地（auto-plan #2 p1-search，两 commits）**：① 搜索核心服务（crates/latermd-app/src/search.rs，`f289e54`）——SearchService 三原语（spawn/try_recv/cancel），后台线程 ignore::WalkBuilder（require_git(false) 遵循 .gitignore）+ grep-searcher LineIter 行迭代 + regex::bytes::Regex 逐文件匹配，sync_channel(256) 背压流式回传，AtomicU64 代际号取消；② 侧边栏搜索 UI 与跳转接线（`5b5c976`）——SearchState 状态机 + 300ms 去抖（std::Instant 手写，零新依赖）+ 流式结果列表 + 点击结果打开文件并跳行（line_to_byte 超界钳制）；③ **依赖登记**：ADR-004 补登 grep-searcher 0.1.17 与 regex 1.13.1，decisions-pending #7 记录「regex::Regex 未实现 Matcher trait，改用同 crate LineIter + bytes::Regex，不引入 grep-regex」的实现岔路。六项门禁本机全绿（199 测试通过） |
| 2026-09-25 | **P1 latermd-ai 基础落地（auto-plan #3 p1-ai-base，两 commits）**：① latermd-ai 核心 crate（`a50f595`）——`AiProvider` trait（std 线程 + mpsc 流式，零 tokio，`#![forbid(unsafe_code)]`，无 egui 依赖落实为无该依赖项）+ `MockProvider`（按关键词分流两套中文 Markdown 脚本，33 块 × 100ms ≈ 3.3s）+ OpenAI 兼容 adapter（ureq 3.4.2 阻塞 HTTP 读 SSE，解析落在纯函数 `parse_openai_sse`，`[DONE]` 终止）；**依赖登记**：ureq 3.4.2 进 ADR-004 §2 + 修订记录（reqwest blocking 内建 tokio runtime，与零 tokio 口径冲突，故选 ureq）；**新契约**：decisions-pending #9 —— done=true 且 delta 非空 = 错误描述（不写入文档），端点/模型可经 `LATERMD_AI_BASE_URL` / `LATERMD_AI_MODEL` 覆盖；② app 侧流式接线（`fea8e44`）——菜单「AI: Mock 流式续写」+ `Message::AiChunk/AiDone/AiFailed` 归约（rope 末尾追加 + 防重入标志）；**实现岔路**：decisions-pending #10 —— 逐块 heal 写 rope 会把闭合标记永久写进文档，改为预览帧整篇 heal（`.heal(true)`），rope 只收原始 delta（完整文档上 heal 为恒等变换，非流式行为不变）；流式后首次 Ctrl+Z 一步回退整段 AI 续写（快照式 undoer 固有行为，测试钉住）；红线①稳定 widget id 复核通过（`preview-md` 常量 id，不含 content 长度）；bench 复测：流式 append 中值 8.73ms，longdoc 500/2000/10000 行 = 39.3/149.4/756.1ms（10000 行超 16.7ms 帧预算，流式追加 O(n) 硬约束仍在）；六项门禁本机全绿 |
| 2026-09-25 | **P1 ai:// 链接协议与 ```ai 指令块落地（auto-plan #4 p1-ai-links，两 commits）**：① `ai://` 链接协议（`47621f1`）——新增纯解析模块 `crates/latermd-app/src/ai_link.rs`（`ai://write?prompt=<urlencoded>` → Mock 流式续写；未实现动作 / prompt 解析失败 → 状态栏提示；非 `ai://` 前缀不拦截），app 侧注册 vendored LinkHandler（link_style 明暗两档紫罗兰换色 + click 拦截），`Message::AiLinkClicked` 与 AiStart 归约共用 `start_ai_stream_with_prompt` 同一入口防重入；协议语义定稿 decisions-pending #11（prompt 原样透传 provider、`+` 不当空格、空 prompt 视为缺失），percent-encoding 2.3.2 登记 ADR-004（Cargo.lock 既有传递依赖升为直接依赖）；实测 vendored `LinkStyle.underline` 无任何读取点（hover 下划线在 layout.rs:197 对全部链接无条件绘制），样式区分由颜色承担，字段生效需改 vendor（①类，本轮未做）；② ```ai 指令卡（`b9d5070`）——实测 vendored `is_block_widget` 仅作用于 `Token::Link` 的 href、够不到围栏代码块（roadmap 阶段 3 行文已随之订正为 `is_block_code_widget` → `block_code_widget`），按 decisions-pending #12 给 vendored LinkHandler 新增代码块级 block widget 扩展点（①上游可合，`needs_segmentation` / `build_layout` / `render_token_range` 三处同步 + tests/block_code_widget.rs，登记 vendor/README.md；子树内差异行经 `602e485` 唯一化到该文件），app 侧渲染指令卡（指令文本 240 字符截断 / 执行按钮 / 未执行·进行中·已完成状态行，键控口径 #13：指令文本匹配 `AiState::last_prompt`），点击产出 `Message::AiLinkClicked` 与 ai:// 链接同一条归约，卡片 id 按文档序号构成、编辑不变（AGENTS §6.7）；六项门禁本机全绿（vendor 100 + app 102 测试）；真机点击冒烟挂账人工（样例：crates/latermd-app/examples/ai-link-sample.md、ai-instruction-block-sample.md） |
| 2026-09-25 | **P1 AI commit message 与摘要大纲落地（auto-plan #5 p1-ai-tools，两 commits）**：① AI commit message（`9906548`）——latermd-ai 新增 commit.rs（truncate_diff 16KB 字节预算、不拆 UTF-8 的截断 + commit_message_prompt，输出要求一行中文 conventional subject）与 `MockProvider::mock_commit_subject` 同步方法（按 diff 关键词 new file mode/README/Cargo.toml 等合成前缀+动作+文件名）；app 侧新增 git_diff.rs（std::process 跑 git diff，staged 优先回落 working tree，core.quotepath=false 保中文文件名可读）+ Command::AiCommitMessage 菜单 + AiCommitRequested/AiCommitSuggestion/AiCommitDismissed 归约（复用防重入闸与 Message 通道，网络路径留 TODO）+ 建议浮窗（显式「复制」按钮走 Context::copy_text，不自动覆盖剪贴板）；仓库定位等岔路登记 decisions-pending #14；② AI 摘要（`80573b1`）——latermd-ai 新增 summary.rs（summary_prompt + truncate_document 32KB 字节预算按 UTF-8 字符边界截断，与 truncate_diff 共享 truncate_bytes）+ `mock_summary` 按标题/列表关键词抽 3-5 条要点并直产「> - …」引用块形态、stream_complete 按摘要指令头嗅探走 60 字符分块流式；latermd-md 新增 heading_section_span（基于 outline 的 AST 层节定位，前导空行吸收经探针实测）；app 侧 request_summary 防重入 → AST 定位删旧「## AI 摘要」节 → 复用 AiChunk/AiDone 流式通道追加；「引用块直插文档末尾而非浮窗可选插入」等岔路与已知边界登记 decisions-pending #15；零新依赖（未动任何 Cargo.toml，ADR-004 无需登记）；六项门禁两模块各自本机全绿（latermd-ai 15 + latermd-app 108 / 17 个 test target 全 ok） |
| 2026-09-25 | **P2 Git 只读集成落地（auto-plan #6 p2-git，两 commits + 一评审修复）**：① latermd-git 核心 crate（`e6ba523`）——git2 0.21.0 只读封装（零 egui 依赖、实现代码无 unwrap）：status（untracked 递归、按路径排序、M/A/U/D/? 五码，git CLI --short 语义）、log（TIME+TOPOLOGICAL 组合序——纯 TIME 同秒顺序不稳定实测后修正、limit、空仓库返回空表）、diff_file（HEAD vs 工作区、64KB 字符边界截断、二进制占位）、blame_file（行级归属 HEAD 提交，工作区未提交行不参与——libgit2 限制）、checkout_file（唯一写操作，CheckoutBuilder path+force，须 UI 确认模态）及 8 单测（非 git 目录全 API 返回 Err 不 panic）；**依赖岔路**：未照抄 ADR-004 的 vendored-openssl 组合，走 default-features=false + vendored-libgit2（P2 无 fetch/push、Cargo.lock 零 openssl 面），连同 U=unmerged/?=untracked 口径、仓库根严格 open 不向上 discover 登记 decisions-pending #16；② Git 面板与文件树角标（`00e8a6c`，logic/ui 二分照 adr-005）——侧边栏 Git 页：平铺改动列表彩色角标、点击读只读 diff（等宽 +/-着色）、回滚经确认浮窗（egui 0.36 无内建阻塞模态，与 AI commit 建议浮窗同模式 +「不可撤销」警示）、历史折叠区短 hash+subject+相对时间；Files 页 md 文件名旁彩色 Git 角标；latermd-git 补 discover()（文件树根为仓库子目录时向上探测 .git，严格根口径不变、UI 层换算）；刷新 3s 节流轮询 + 触发式（换根/切 Git 页/回滚完成），非 git 目录降级停轮询；岔路登记 decisions-pending #17；③ 评审修复（`1df9217`）——回滚目标为当前文档时 dirty 缓冲分流保留未保存稿、确认模态针对性警示、StatusSnapshot 500 条截断（decisions-pending #18/#19）。六项门禁本机全绿（287 测试通过）；真机仅 Linux X11 冒烟 12 秒无 panic，Git 页交互由 125 个 latermd-app 无头 UI 测试覆盖，Win/mac 三平台未实测 |
| 2026-09-26 | **P2 凭据管理落地收口（auto-plan #7 p2-creds，两 commits 经 PR #20 携带合入 main）**：① latermd-creds 核心 crate（`adbf43c`）——trait CredentialBackend（内存实现 + keyring 4.2.0 实现，生产顶层函数走 keyring），顶层 set/get/delete/has_secret 与专用入口 ai_api_key/set_ai_api_key（系统凭据 > LATERMD_AI_API_KEY 环境变量，后端不可读时降级环境变量保住逃生口），CredentialError 文案经 redact 消毒绝不含凭据值；8 单测全过且本机（Deepin + Secret Service）keyring 真实跑通写/读/幂等删全链路；keyring 4.2 选线、get_secret 返回 Result<Option>、env 回退测试注入、测试值口径四岔路登记 decisions-pending #20；② 设置面板 AI Provider 区（`41dc89c`）——ai_key.rs 三态状态（草稿/已配置/后端可用）+「设置 → AI Provider…」菜单入口 + 密码浮窗（掩码输入/保存/清除/三态状态行，后端不可用提示回退 LATERMD_AI_API_KEY），key 闸门插在全部四个 AI 命令归约最前面（无 key 且需 key 时零副作用拦截，Mock provider 恒 false 直通）；浮窗形态/状态分组/闸门位置三岔路登记 decisions-pending #21。roadmap「当前位置」P2 行随标「P2 全部完成」，P2 收口 |
| 2026-09-26 | **界面打磨批次 + AI 配置页（[ui-polish.md](ui-polish.md)，PR #20 合入）**：自绘矢量图标体系（`ui/icons.rs`，零字体依赖、随主题取色）、工具栏三段分组（文件/视图/AI）+ 齿轮入口、新增底部状态栏（路径·行列·字数·主题·后端·AI·MCP）、设置对话框升级为「左侧分页 + 右侧内容」四页（外观/快捷键/AI/MCP）、快捷键可改绑并落 `keymap.json`（撞键拒绝、逐行重置与全部重置）、AI 配置页（`ai_config.rs`：provider / 接口方式 / Base URL / 模型 / Temperature / Top-P / Max tokens / System prompt / 超时 / 流式，落 `ai.json`，key 仍只走系统凭据）。设计规格见 ui-polish.md |
| 2026-09-26 | **MCP server 落地（[mcp-plan.md](mcp-plan.md) 阶段 ①–④ 全完成）**：① 检索核心下沉为新 crate `latermd-search`（方案 A：侧边栏与 MCP 共用同一实现，`.gitignore` 语义与截断上限不漂移；新增同步入口 `search_sync` 与 `list_files`，glob 走 `ignore` 的 override 匹配查询而非 `builder.overrides()` —— 后者只筛文件、目录照旧产出）；② 新 crate `latermd-mcp`（不依赖 egui，守铁律二）：JSON-RPC 2.0 + `initialize`/`tools/list`/`tools/call`，五个**只读**工具 `search_docs` / `read_document` / `outline` / `list_files` / `git_status`，路径 `canonicalize` + 前缀校验（`..`、符号链接、绝对路径越界一律拒绝），未设文档库根时统一报「未设置文件树根目录」；③ 双通道：stdio（行分隔 JSON，`latermd --mcp-stdio`，headless 根取 `LATERMD_MCP_ROOT`）与 HTTP（`127.0.0.1:<port>`，GUI 进程内常驻，按 `Accept` 协商 SSE 或 JSON）；④ app 接线：设置页 MCP 页（启用开关 / 端口 / 五工具权限 / 状态行 / 调用计数 / 保存，与 AI 页同款草稿-保存分工）+ 状态栏 MCP 段 + `Message::McpConfigSaved` 归约 + 换根走共享句柄（服务不重启）。**默认关闭**，端口区间 1024–65535，只绑回环地址。实测：真进程 stdio 冒烟跑通 initialize/tools/list/一次真实检索（8 条命中）；`state::tests::mcp_config_saved_starts_server_and_answers_over_http` 从保存配置走到真实 TCP 应答。六项门禁全绿（app 189 / mcp 33 / search 14 测试） |
| 2026-09-26 | **皮肤批次 B 落地（roadmap P2.5 收口）**：① 三态主题（浅色/深色/跟随系统）——`ThemeMode::System` 只在 `resolve` 处落到确定值，检测经 `dark-light` 3.0.0，结果缓存 + 1 秒节流轮询（非跟随模式零轮询），检测不到回落上次手动值并在设置页明示（roadmap 风险 #8）；② 自定义皮肤文件——`themes/*.ron` 存 `MarkdownStyle`（`ron` 0.12.2），皮肤文件是唯一事实源、`settings.json` 只存名字，启动扫描目录、设置页可下拉选择并把当前样式导出为新皮肤（文件名清洗挡住 `..`）；③ 视觉打磨——标准/紧凑两档密度，以 egui 出厂 Style 为基准缩放间距/控件尺寸/圆角/滚动条（不基于当前值累乘、不动字号）。新依赖 `dark-light` 3.0.0 与 `ron` 0.12.2 已按 decisions-pending #4 登记进 ADR-004。六项门禁全绿（app 198 测试）。岔路与边界见 decisions-pending #24 |
| 2026-09-26 | **Live Preview v1 落地（P3 深水区 #9）**：① `latermd-md` 新增 `blocks()` —— 按空行切段落、块级 token（代码块/表格/分隔线/标题）独占一块、列表每项一块，字节区间**连续覆盖全文**（安全底线：光标块是被编辑的区间，落在块外的字节会在敲键时静默丢失）；两个 vendored span 坑已记档：平铺 span 会吸收前一块尾部换行（内容起点改用 token 文本在 span 内定位）、CodeBlock span 只到内容末尾（块吃到下一块内容起点后自然含住闭合 ```）。② app 侧新增 `live.rs`：`RenderMode` 一个标志分派源码/Live，活动块编辑经 `BlockBuffer`（egui `TextBuffer` 适配）按「块内偏移 + 块首偏移」落回**同一个 `EditorBuffer`**，切模式零搬运（单测钉住文本/修订号/dirty 不变）；↑ 在块首去上一块末、↓ 在块尾去下一块首，Home/End 仍走 TextEdit 内建；点击非活动块即进入编辑（落点取块末）。③ 命令 `ToggleLivePreview`（Cmd/Ctrl+/）进菜单与快捷键页。块是**整块**切换，v2 才做内联标记半隐藏。六项门禁全绿（app 207 / md 15 测试）。岔路与边界见 decisions-pending #25 |
| 2026-09-26 | **`[[wikilink]]` 双向链接落地（P3 #10 的一半）**：① `latermd-md` 新增 `wikilinks()` / `expand_wikilinks()` —— 把 `[[目标]]`（支持 `[[目标\|显示名]]`）展开成 `[显示名](<wiki://目标>)`，**只改渲染文本**（存 `PreviewState::rendered`，随修订号重建），源码一字不改；围栏代码块内的 `[[…]]` 不算链接（否则 Rust 的 `arr[[0]]` 会被改坏）。② 点击由既有 LinkHandler 拦 `wiki://`（与 `ai://` 同一通道），颜色取青绿与 AI 紫罗兰区分；归约走 `filetree::find_by_name` 按文件名在库内找文档并打开（去扩展名 + 忽略大小写 + 嵌套目录，遍历复用 `latermd_search` 故同样尊重 `.gitignore`），找不到或未设文档库根都落提示行。六项门禁全绿（app 213 / md 18 测试）。口径与边界见 decisions-pending #26。**大纲预览跳转未做**：需 vendored 侧暴露各 section 的 y 坐标（①上游可合类改动），已登记为 P3 剩余项 |
| 2026-09-26 | **新增 [acceptance-checklist.md](acceptance-checklist.md) 人工验收清单**：把自动化门禁替代不了的部分（打 tag 前的静态前置、发布链路观察点、Win11 / macOS 14 / Linux 三平台装包与渲染后端、**IME 三条判据**、稳定性长跑、本轮新功能真机抽查、发布后 cask sha256 与证据回填）整理成可勾选清单，每项都写死判据，并给出「什么算挂」的速查（IME 任一条不过 = M0 头号风险命中，按 ADR-001 §2.5 讨论备选方案，不硬修 egui）。P0 出口即「勾完这份清单」 |
| 2026-09-26 | **大纲预览跳转落地（P3 #10 收口）**：vendored 新增 `SectionAnchor` 与 `section_anchors()`（**①类**改动，纯新增能力）—— `MarkdownLabel` 把内容画进单个 galley，调用方原本无从知道某一节落在第几像素，无法做「滚动到这一节」；现在在 `render_galley` 两条分支把各 section 的顶部 y 记进 `ui.data`，app 侧查表即可。行定位有两个坑：空行贡献 0 个字符但占一行（不按 `max(1)` 推进会让后一节的 y 反向）、游标必须按 section 重新从顶部走。app 侧：大纲点击的 span 同时驱动编辑器跳光标与预览滚动，滚动目标存 `PreviewState::scroll_target`（UI 关注点，绘制消费一次 —— 否则每帧把预览拽回目标，用户再也滚不动）。六项门禁全绿（app 215 测试），vendor `check.sh` 六项全绿。口径与边界见 decisions-pending #27 |
| 2026-09-26 | **UI 换 WorkBuddy 风外壳（坤哥指定）**：强调色紫罗兰 → 飞书系蓝（浅 #3370FF / 暗 #6C9FFF，AI 元素保留紫罗兰）；新增 `theme::shell_tokens` + `apply_shell()` 把侧栏灰 #F2F3F5 / 内容白 / 文字 #1F2329 / 浅蓝选区 / 控件圆角 6 投影进 egui 两套 style（一次投影，不逐帧作废缓存）；面板分区靠底色而非硬边框；页签选中态改「浅蓝底 + 蓝字 + 底部 2px 蓝条」。实现坑：TextEdit 底（`extreme_bg_color`）与代码块底（`code_bg_color`）必须分开给。llvmpipe 实跑截图 + 像素采样验证（918 px 命中强调蓝）。口径见 decisions-pending #28 |
| 2026-09-26 | **多标签页落地（auto-plan #11 multi-tabs，用户追加需求）**：① 归约迁移（tabs-core，`3753939`）——AI 流生命周期从「切标签即作废」反转为「绑定发起标签」：`State::ai_active_tab` 发起时锁定当前 tab id，AiChunk/AiFailed 按该 id 定位写入目标标签（与 active 无关；id 失效则丢弃并 abort，fail-safe 防静默写错 tab），AiDone/AiFailed/关发起标签时清除，防重入口不变；`FileCmd::Open` 三入口（菜单打开/文件树/搜索跳转）统一归约 `open_path`（原 `open_from_file_tree` 改名）：`find_by_path` 命中即激活、未命中开新标签；文件菜单补 TabNext/TabClose 可发现性入口；任务点名的六场景单测齐备（同路径单标签/切标签编辑隔离/AI 写发起标签/关非 active 指针钳制/关脏标签确认链/全关回空标签）。② 标签条溢出策略（tabs-ui，`d1ff599`）——`horizontal_wrapped` 换行改为 `egui::ScrollArea::horizontal` 单行水平滚动（crates/latermd-app/src/ui/tabs.rs，零新增依赖），补带检出力的回归测试（临时回退 wrapped 实现确认测试会红）；`DISPLAY=:0` 真实 GUI 冒烟走通开两文件/切换/改脏/关脏标签确认模态全链路，窗口标题联动（文件名 + dirty 星号）逐项确认。与任务描述的口径冲突（Message 形态、确认目标存 `TabsState::confirm_close` 而非消息载荷、`State.tabs` 封装为 TabsState、路径按落盘身份逐字节比较而非 fs::canonicalize）均以现状为准，登记 decisions-pending #23。门禁：三轮 clippy -D warnings（default/no-default-features/all-features）、`cargo test --workspace --all-features`（353 通过 0 失败）、`cargo doc --no-deps --all-features` 无警告；tabs-core 会话另过 `cargo fmt --all --check`（tabs-ui 会话 fmt 失败两处均在并行棒次文件、非本功能产物，rustfmt --check 对 ui/tabs.rs 通过） |
