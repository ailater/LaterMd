[package]
name = "latermd-editor"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rope-backed editor buffer with cursor offset conversion (no UI deps)"

[dependencies]
ropey = "1.6.1"
