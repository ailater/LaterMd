[package]
name = "latermd-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Local MCP server exposing read-only document tools (no UI deps)"

[dependencies]
